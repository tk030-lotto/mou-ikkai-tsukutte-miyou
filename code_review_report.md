# 「もう一回作ってみよう。ツール」コードレビューレポート

実施日時: 2026-08-26
レビュー対象: `c:\Users\tk030\Desktop\もう一回作ってみよう。ツール` (`index.html` / `app.js` / `style.css`)
レビュー観点: 仕様適合性・バグ・セキュリティ・アクセシビリティ・コード品質・保守性

> 注: `.clinerules` が参照する `knowledge/protocol.md` および `knowledge/quality-audit.md` は本プロジェクトに存在しなかったため、`SKILLS.md` の記載内容を根拠に手動レビューを実施した。

---

## 総合判定: 【 B+ （良好・修正推奨項目あり）】

過去の監査レポート（`audit_report.md` S評価）の内容と照らすと、仕様適合性・Zero-Dependency・DOM整合性は自動検証でも確認できた。一方で、監査では検出されていない **UX上の実バグ2件** と **アクセシビリティの欠落2件** を発見した。

### 自動検証結果（全て PASS）

| 検証項目 | 結果 |
|---|---|
| `app.js` 構文解析（Node.js） | ✅ OK（パース成功・エラー0件） |
| JS→HTML の ID 参照整合性 | ✅ 21参照すべて解決・未定義参照なし |
| チップ `data-modifier` ⇔ `MODIFIER_TEMPLATES` キー一致 | ✅ 5/5 完全一致 |
| CSS 波括弧バランス | ✅ 155/155 |

---

## 🟥 重要度：中（修正推奨）

### 1. 「もう一度最初から」で確認ダイアログが二重表示される

- **該当箇所**: `app.js:488-496`（`btn-restart-all` ハンドラ）、`app.js:226-228`（`clearDraft()`）
- **内容**: `btn-restart-all` ハンドラが `confirm()` を表示した後、内部で呼び出す `clearDraft()` が**入力がある場合に再度 `confirm()` を表示**する。利用者は同じ意味の確認に2回答えさせられる。
- **影響**: UX低下（二重確認による煩わしさ）。
- **修正案**: `clearDraft(force)` のように確認スキップフラグを追加し、restart-all 経由では confirm を1回のみ表示する。

```js
// app.js:488 — 1つ目の confirm
if (confirm('最初からやり直しますか？（入力した下書きもリセットされます）')) {
  clearDraft();  // ← 内部でさらに confirm('入力中の下書きをすべて消去しますか？') される
```

### 2. コピーボタン連打時、先のボタンのラベルが「コピー完了！」のまま戻らない

- **該当箇所**: `app.js:293-307`（`animateCopyButton`）
- **内容**: 単一の共有タイマー `copyButtonTimer` を使用しており、「上部コピー」→即「下部コピー」の順で押すと、先のボタンのテキスト復元処理が `clearTimeout` によりキャンセルされる。
- **影響**: 先に押したボタンのラベルが**ページ再読み込みまで「コピー完了！」表示のまま残る**。
- **修正案**: 新規アニメーション開始前に前のボタンの状態を復元する、またはボタンごとにタイマーを持たせる（`WeakMap` 等）。

---

## 🟧 重要度：低（改善推奨）

### 3. `showToast` が message をエスケープせず innerHTML に挿入

- **該当箇所**: `app.js:328`
- **内容**: 現状の呼び出しはすべて固定文字列のため**脆弱性ではない**。ただし将来動的メッセージ（文字数やユーザー入力を含む等）を渡すと XSS リスクになる。

```js
toast.innerHTML = `${iconSvg}<span>${message}</span>`;
```

- **修正案**: 要素生成 + `textContent` 併用方式へ変更。

### 4. バリデーション条件とエラーメッセージの不一致

- **該当箇所**: `app.js:349-350`
- **内容**: 条件は「3項目すべて空」だが、メッセージは「作っていたもの、または困っていること」のみ言及。「当初の目的」だけ入力して送信した場合、実際には成功するのに文言上は対象外に見える。

### 5. ステップインジケータが「当初の目的」の入力を反映しない

- **該当箇所**: `app.js:113-126`（`updateStepProgress`）
- **内容**: Step 3（当初の目的）は problem 入力時に active になり、goal の入力状態は一切反映されない。「到達」なのか「完了」なのか挙動が一貫していない。

### 6. アクセシビリティ欠落

| 項目 | 該当箇所 | 内容 |
|---|---|---|
| `prefers-reduced-motion` 未対応 | `style.css` 全般 | pulse/shake/slide-in アニメーション多数に対し、動き軽減設定への配慮がない |
| `:focus-visible` 未定義 | `style.css` | キーボード操作時のフォーカス表示がブラウザ依存 |
| contenteditable に aria-label なし | `index.html:282` | `#prompt-content` にアクセシブルネームがない |
| 無機能ボタン | `index.html:48` | `#theme-indicator` が `<button>` なのにクリック不可・リスナーなし → 誤解を招くため `disabled` 化か非インタラクティブ要素化を推奨 |
| ビュー切替時のフォーカス管理なし | `app.js:88-108` | 結果画面見出しへフォーカス移動がなく、スクリーンリーダー利用者が遷移を認識しにくい |

### 7. contenteditable への貼り付けで装飾 HTML が混入し得る

- **該当箇所**: `#prompt-content`（`index.html:282`）
- **内容**: paste イベントで `text/plain` に制限していない。コピー時は `innerText` 取得のため実害は小さいが、貼り付け操作でスタイル付き HTML が混入し表示が崩れる可能性がある。

---

## 🟦 重要度：情報（設計判断の確認事項）

8. **手動編集済みプロンプトがチップクリックで破棄される**（`app.js:418`）— `isCustomEdited = false` により編集内容が失われる。「即反映」仕様（仕様書 §14）として意図的と思われるが、マージ方式も検討の価値あり。
9. **`state.inputs` と DOM 値の二重管理** — 現状整合は保てているが、単一ソース化するとより安全。
10. **インラインスタイル**（feature-icon / strategy-letter の色指定、`index.html:86,98,108,319,326,333,340`）— デザイントークン（CSS変数）化されている他部分と方針不統一。
11. **`required` 属性が `novalidate` により無効化**（`index.html:151`）— 冗長だが害なし。JS側バリデーションで担保済み。
12. **Git 未コミット変更あり**: `SKILLS.md`, `.agents/AGENTS.md`（修正）、削除済み txt ファイル、未追跡 `.agents/agents/`。マイクロコミット原則に従い整理を推奨。

---

## ✅ 高く評価できる点

- **Clipboard API + `document.execCommand` フォールバック + `window.isSecureContext` ガード**の堅牢な実装（`app.js:251-287`）
- **localStorage 操作の全面 try/catch**（プライベートモード・quota 超過でもクラッシュしない、`app.js:195-240`）
- **iOS 自動ズーム防止**: textarea/placeholder を `1rem`(16px) 統一（`style.css:517,533`）、`maximum-scale=5.0` でユーザー拡大は許容
- **Ctrl/Cmd+Enter 送信時の `e.preventDefault()`**（`app.js:452-457`）、IIFE + `'use strict'` によるグローバル汚染防止
- **XSS 入口が存在しない設計**: localStorage 値はすべて `.value` 経由で代入、プロンプトは `innerText` 描画
- **タイマーライフサイクル管理**: focus/toast/copy の各タイマーで重複起動時に `clearTimeout` を実施（※第2項の課題を除く）
- **OGP/Twitter Card・SVG favicon・LICENSE リンクの `rel="noopener"`** 等の公開品質
- 仕様書 §19 の完成条件をすべて満たし、仕様外機能（AI API連携・サーバ保存等）の混入なし

---

## まとめ

本プロジェクトは小規模ながら防御的プログラミングが行き届いた高品質な実装である。発見した課題はいずれも致命的なものではなく、🟥2件のUXバグ修正とアクセシビリティ補完（reduced-motion / focus-visible）を実施すれば **S 相当** に到達可能と判断する。

| 判定 | 件数 | 内訳 |
|---|:---:|---|
| 🟥 中（修正推奨） | 2 | 二重confirm、コピーボタンラベル残存 |
| 🟧 低（改善推奨） | 5 | innerHTML挿入、メッセージ不一致、ステップ進捗、a11y欠落、paste制御 |
| 🟦 情報 | 5 | 設計判断確認事項 |
